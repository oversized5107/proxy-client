[project]
name = "proxy-client-tools"
version = "0.1.0"
description = "Local Python tools for proxy-client configuration files."
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0.2",
    "rich>=13.7.1",
]

[tool.uv]
package = false

